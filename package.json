{
  "description": "Starter WordPress theme",
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "gulp": "^3.9.1",
    "gulp-markdox": "^0.1.1",
    "gulp-phpcs": "^1.1.1",
    "gulp-rename": "^1.2.2",
    "gulp-shell": "^0.5.2",
    "gulp-watch": "^4.3.9"
  },
  "browserify": {
    "transform": [
      "browserify-shim"
    ]
  },
  "browserify-shim": {
    "jQuery": "global:jQuery"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "private": false,
  "scripts": {
    "test": "gulp && git status | grep 'working directory clean' >/dev/null || (echo 'Please commit all changes generated by building'; exit 1)"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/moxie-lean/lean-theme"
  }
}
